{% extends 'admin.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Création d'une note</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="waves-effect waves-light btn" type="submit" value="Créer"/>
        {{ form_end(form) }}

        <ul>
            <li>
                <a class="waves-effect waves-light btn green" href="{{ path('note_index') }}">Retour à la liste</a>
            </li>
        </ul>
    </div>
{% endblock %}
